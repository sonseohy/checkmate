package com.checkmate.domain.contractclause.repository;

import com.checkmate.domain.contractclause.entity.ContractClause;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContractClauseRepository extends JpaRepository<ContractClause, Integer> {
}
